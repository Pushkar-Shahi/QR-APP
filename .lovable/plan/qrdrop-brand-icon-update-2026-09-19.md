# QRDrop brand icon update

## Changes
- Store the supplied icon as the app’s navigation brand asset.
- Replace the existing generated QR-style mark beside “QRDrop” with the supplied icon.
- Create a properly sized favicon/web app icon from the same artwork and remove the default favicon.
- Preserve the existing title, layout, light/dark styling, and footer privacy message.

## Verification
- Confirm the navigation icon renders clearly and the page has no Lovable branding.
- Confirm the browser favicon points to the new icon and the preview builds successfully.
